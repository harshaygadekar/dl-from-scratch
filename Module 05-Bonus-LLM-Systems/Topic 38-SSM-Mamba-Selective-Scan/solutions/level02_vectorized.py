import numpy as np
from level01_naive import selective_scan_naive


def selective_scan_vectorized(a, b, x):
    # Keep semantics identical to baseline in this educational scaffold.
    return selective_scan_naive(a, b, x)

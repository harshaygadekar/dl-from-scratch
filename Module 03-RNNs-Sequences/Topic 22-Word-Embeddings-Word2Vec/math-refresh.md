# Math Refresh

- Keep sequence shape conventions explicit (e.g. `(T, B, C)` or `(B, T, C)`).
- Validate hidden-state updates with small deterministic examples.
- Add numerical stability guards (softmax/log operations).
